# Overrides for the docker-compose PoC. Loaded after app-config.yaml.
app:
  baseUrl: http://localhost:7007

backend:
  baseUrl: http://localhost:7007
  listen: ':7007'
  # SQLite file-backed: no Postgres container needed for the PoC.
  # The DB file lives in /app/backstage-data (created by the backend at
  # startup); mount a volume there if you want it to survive rebuilds.
  database:
    client: better-sqlite3
    # A DIRECTORY, not a file: Backstage gives each plugin its own sqlite
    # database and refuses a single `connection.filename` for the base
    # connection. Same constraint the Helm chart's ConfigMap encodes.
    connection:
      directory: /app/backstage-data

auth:
  providers:
    # Required because the image runs NODE_ENV=production, where the guest
    # provider is off by default. PoC only — every visitor becomes a signed-in
    # guest.
    guest:
      dangerouslyAllowOutsideDevelopment: true

catalog:
  locations:
    # Paths are relative to the backend process working dir inside the
    # container (/app), NOT packages/backend like in local dev.
    - type: file
      target: ./examples/entities.yaml
    - type: file
      target: ./examples/release-entities.yaml
    - type: file
      target: ./examples/springboot-service.yaml
      rules:
        - allow: [Component, System, API, Resource, Group]

    # Local example template
    - type: file
      target: ./examples/template/template.yaml
      rules:
        - allow: [Template]

    # Local example organizational data
    - type: file
      target: ./examples/org.yaml
      rules:
        - allow: [User, Group]
