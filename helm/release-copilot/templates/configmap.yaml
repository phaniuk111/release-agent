apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "release-copilot.fullname" . }}-env
  labels:
    {{- include "release-copilot.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
